[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "mcp[cli]>=1.28.0",
    "pydantic>=2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
